movies_json = []

@movie.each do |movie|
  movie_hash = {
    id: movie.id, 
    title: movie.title, 
    year: movie.year, 
    plot: movie.plot, 
    director: movie.director, 
    # language: movie.language
  }

  movies_json << movie_hash

end

return movies_json